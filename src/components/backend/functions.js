import {View, Text} from 'react-native';
import React from 'react';
import {MAPBOX_API} from '@env';
import {useDispatch} from 'react-redux';
import {setPickupAddress} from '../../slices/navSlice';
import {View, Text} from 'react-native';
import React from 'react';

const functions = () => {
  return (
    <View>
      <Text>functions</Text>
    </View>
  );
};

export default functions;
